# Case 1 机构侧收款引导优化 · 三层对比分析

> **测试对象**：⚫前置指引 · 设计自驱 · Claude-Opus-4.8
> **载体**：国际汇款「After Sending（汇到微信后）」状态页 → 目标从「状态展示页」升级为「交易推进页」
> **实验形式**：三臂累加消融——同一需求、同一模型，原则层逐层叠加：`仅 L1` → `L1+L2` → `L1+L2+L3`
> **评判方式**：每臂各自跑「召回 → 方案 → 自评」四步，本轮为 **AI 自评**，尚未做盲评
> **预期命中原则（人工预埋）**：L1-05/07/08/13 · L2-01/06 · L3 指引-01/03/07

---

## 一、结论速览

| 维度 | 结论 |
|---|---|
| 召回 | 前置指引 3 条预期**全部命中**；但实际大幅**跨类召回**（L3 拉入通知/状态/选择共 14 条）——不是错召，是**这一格本身就不是单一服务类型** |
| L1 的作用 | **主干几乎全靠 L1 立住**：信息架构翻转、自收/他收分场景、时效 framing、修正误导文案 |
| L2 的边际 | **有清晰加分**，且是 L1 完全没有的维度：可追溯不丢、汇率口径、双币种辨识、在途可预期、发错防错 |
| L3 的边际 | **有加分但递减、且冗余上升**：通知口吻、跳转前预览、内容外推、专属链接安全、降级路径——更"跨境业务化"，但引入大量与 L1/L2 重叠的条目 |
| 最高价值产出 | 三臂**独立收敛**指向同一批缺口（尤以「主动推进/行动优先」三次全中），这是本 case 最可信的信号 |
| 最大短板 | 三臂公认**可执行性最弱**——原则都是"优先 X"的方向判断，没有可检验的验收阈值 |

---

## 二、召回层对比

### 各臂召回数量

| 层 | 预期命中 | 仅 L1 | L1+L2 | L1+L2+L3 |
|---|---|---|---|---|
| L1 | 4 条 | 13 条 | 13 条 | 9 条 |
| L2 | 2 条 | — | 6 条 | 5 条 |
| L3 | 3 条（均 指引） | — | — | 14 条（跨 通知/指引/状态/选择 4 类）|

### 关键发现：前置指引触发准，但页面是"多类型叠加"

- **前置指引本身召回 100%**：预期 指引-01/03/07 三条，L1+L2+L3 臂全部命中（还多召 指引-04/06）。**说明前置指引的触发字段是好用的，不需要改。**
- **真正的信号在跨类**：L3 臂还召回了 通知-01~04、状态-02~04、选择-02。AI 自己给出了原因——
  > 「这页同时是 **状态反馈 + 通知触达 + 前置指引**，谁该当页面主角，没有页面级的编排原则。」
- 这**不是错召**，而是印证了四方评审早已指出的问题：**9 类服务类型不互斥、更像标签体系而非单选分类**。一个真实订单页天然多类型叠加。

> **对方法的启示**：召回核对时，"实际 ≠ 预期"要先分清是**触发字段问题**还是**这一格本就是多类型页**。本 case 明显是后者——不要据此去改触发字段，而要正视"单选归类"前提在真实页面上会失效。

---

## 三、加分层对比：每一层让方案多做对了什么

> 逐臂比对「设计说明」，剥出每层新增的、上一层没有的设计决策。

### 仅 L1 —— 立住了整个骨架与方向

几乎全部主干判断都有 L1 依据，且是真用上、非硬凑：
- 信息架构翻转：行动卡提首屏、Timeline 折叠降级（L1-03/04/05/13）
- 自收/他收分场景渲染，修正 "Share to recipient" 误导文案（L1-08）
- 24h 退回时效前置，framing 为"帮你避损"而非恐吓（L1-11 + L1-02 兜底）
- 时效多线索表达、降级只给本端反馈不谎报对方进度（L1-16/L1-10）

**判断**：这类"设计自驱"需求的**方向层重构，L1 基本能独立扛住**。

### +L2 —— 补上「钱」的信任维度（L1 完全缺位）

L2 带来的增量清晰且不可替代：
- **可追溯不丢**：Timeline 降权折叠但保留"交易进度可追溯"（L2-01）
- **在途可预期**："On its way / within minutes"预期披露、安全兜底条（L2-07）
- **发错对象防错**：他收催快时防止把钱推向错误对象（L2-04）
- 双币种辨识、汇率口径（L2-05/06/02，召回到位）

**判断**：★ **L2 有真实边际价值**，且填的是 L1 结构性没有的"资金信任"维度。

### +L3 —— 业务化落地手法 + 把 L2 的金额维度真正落进方案

L3 带来更贴跨境汇款场景的具体手法：
- 通知 CTA 单一入口 + 熟人口吻（通知-01/02）
- 跳转前预览三步 + 预计到账（指引-01/04 从"原则"变成"打开微信→点领取→约1分钟"）
- 收款人操作步骤外推给落地页（通知-04）
- 专属收款链接"只发给本人"安全提示、降级为复制链接（指引-05）
- **并促使双币种 chip、汇率"已锁定成交时点"真正进入设计说明**（L1+L2 臂召回了却没充分落地）

**判断**：★ **L3 有加分，但边际递减、冗余显著上升**——L3 臂自评点出多处重叠：状态-01↔L1-16、状态-06↔L2-06、通知-04↔指引-01。

### 边际价值阶梯（一句话）

```
L1  = 骨架与方向（信息架构、场景区分、时效 framing）——主干几乎全立住
L2  = 「钱」的信任维度（可追溯、汇率口径、双币种、可预期、防错）——清晰增量
L3  = 业务化手法（通知口吻、跳转预览、内容外推、安全提示、降级）——有加分但递减 + 冗余上升
```

---

## 四、原则体系诊断：三臂独立收敛的缺口（最高价值产出）

> 三个独立上下文各自跑完后，**反复指向同一批缺失**。独立收敛 = 高置信。按收敛程度排序：

| 缺口 | 仅 L1 | L1+L2 | L1+L2+L3 | 置信度 |
|---|:--:|:--:|:--:|---|
| **主动推进 / 行动优先**（体系"只有刹车没有油门"，"状态页→推进页"无原则背书） | ✅ | ✅ | ✅ | ★★★ 三次全中 |
| **伦理护栏可执行化**（L1-02 太宽泛，缺虚假紧迫/暗黑模式的红线清单） | ✅ | ✅ | ○ | ★★★ |
| **时间/时效维度**（L1 全是静态快照，缺倒计时/过期/退回的动态表达） | ✅ | ○ | 部分被 L3 状态-03 补上 | ★★ |
| **降级 / 兜底体验**（能力不可用时不得退回只读态） | — | ✅ | ✅ | ★★ |
| **责任方明确**（进行中状态须说清"现在轮到谁"） | ○ | ✅ | ○ | ★★ |
| **页面级编排 / 多类型仲裁**（一页多职责谁当主角） | — | — | ✅ | ★ 仅 L3 臂暴露 |
| **自动判定 + 纠错**（自收/他收自动分场景的纠错兜底空白） | — | — | ✅ | ★ 仅 L3 臂暴露 |

### 一个有意思的规律：加的层越高，暴露的缺口越"结构性"

- `仅 L1` 暴露的是**能力维度**缺口：缺"油门"（推进）、缺"时钟"（时效）。
- `L1+L2+L3` 暴露的是**元结构**缺口：页面级编排、自动判定纠错。
- 原因：低层缺口被逐层补上后，才暴露出更上层的问题。**说明"补原则"应自下而上——先补主动推进/时效这类基础维度，页面级编排是补完基础后才谈得上的下一轮。**

---

## 五、质量评价趋势

| 维度 | 仅 L1 | L1+L2 | L1+L2+L3 | 趋势 |
|---|---|---|---|---|
| 完整性 | 缺推进/时间/诚实 | 缺自驱型维度 | 缺 4 类元约束 | 随命题深入，缺口从"能力"转向"结构" |
| 一致性 | 好 | L2 内部有重叠 | **重叠冗余明显**（多处双重计数）| **随层级下降**——L3 引入大量与 L1/L2 重复的判断 |
| 可理解性 | 高 | 高 | **优** | 稳定好评，卡片模板有效 |
| 可执行性 | **弱** | **弱** | **弱（最短板）** | 三臂一致：都是"优先 X"方向判断，无验收阈值 |
| 可复用性 | 好 | 好 | 好，但"单选归类"前提在多类型页失效 | — |

**两个跨臂一致的判断**：
1. **可执行性是公认最短板**——"多线索到底几条？""就近突出到什么程度算够？"没有检查点。→ 需给 P0 原则配**检验点 / 反面清单**，把定性判断变成可核对。
2. **一致性随 L3 加入下降**——L3 大量重述 L1/L2 已有判断。→ 需要"同一判断只在最具体层写一次，其余层引用编号"的去重规约。

---

## 六、处置建议（对应验证方案的增/删/改）

### 增（三臂收敛，高置信）
- **【主动推进 / 行动优先】**：结果/状态类页面在存在明确下一步时，可主动承担推进职责，行动区可凌驾状态展示成为主角。→ 三次全中，**最优先**。
- **【助推的伦理边界】**：作 L1-02 的可执行延伸，列明红线（虚假紧迫、暗黑模式、默认勾选、遮挡退出）。
- **【时效/随时间演进的状态】**：涉及时限、倒计时、过期、退回时，"还剩多久、超时会怎样"须随时间清晰诚实演进（跨境高频：锁汇、结算窗口、退款时限）。
- **【降级体验】**：能力不可用时不得损伤核心任务、须保留最小可执行入口；涉资金链接须防误发/防转发。
- （下一轮再谈）**页面级编排 / 自动判定纠错**：属元结构层，待基础维度补齐后再立。

### 改
- **L1-02**：补正向条款——不只说"不能牺牲信任"，还要指导"如何正当地推"。
- **L1-08**：增补"进行中状态须明示责任方（轮到用户/平台/对方）"。
- **L3 状态-02**：补一句"行动待办可优先于状态告知、提升为页面主操作"。
- **L3 指引-03**：从"各角色各看各链路"扩展到"同一入口按场景动态切换文案/CTA/优先级"。
- **去重**：状态-01↔L1-16、状态-06↔L2-06、通知-04↔指引-01——同一判断只在最具体层保留、其余引用编号。

### 删 / 不动
- 本 case 未产生"应删"结论。前置指引触发字段准确，**不动**。
- 召回中被"刻意不召回"的 L1-12/14/15 判断合理，说明 AI 有取舍能力，非遗漏。

---

## 七、方法论提醒（写给后续 case）

1. **本轮是自评，不是盲评**。"新增原则"三臂收敛所以可信；但"L3 到底加了多少分"仍应由**盲评复核**——AI 自评天然倾向于说"原则有用"。建议对本 case 的 A(L1+L2) / B(L1+L2+L3) 两版方案补一次盲评，验证 L3 的加分能否被真人感知。
2. **每臂只跑了一次，没有噪音基线**。三臂差异里，有多少是 L3 带来的、有多少是模型随机性——本轮无法区分。首个 case 建议至少对关键臂跑 3 次。
3. **"预期 ≠ 实际召回"要先判性质**：本 case 的跨类召回不是触发字段坏了，而是"单选归类"前提在真实多类型页上失效。这是分类框架层面的结论，别误改成触发字段问题。
4. **补原则要自下而上**：先补主动推进/时效/伦理护栏这类基础维度，页面级编排留到基础补齐后的下一轮。
